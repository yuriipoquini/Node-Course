"main";let geocode;_839‍.w("./utils/geocode.mjs",[["default",function(v){geocode=v}]]);let forecast;_839‍.w("./utils/forecast.mjs",[["default",function(v){forecast=v}]]);


geocode('Ibiúna', (error, data) => {
    if (error) {
        return console.log('ERROR :(');
    }

    forecast(data.latitude, data.longitude, (nError, nData) => {
        if (nError) {
            return console.log('ERROR :(');
        }

        console.log(data.location);
        console.log(nData);
    })
})