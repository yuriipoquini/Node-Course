"main";let geocode;_50a‍.w("./utils/geocode.mjs",[["default",function(v){geocode=v}]]);let forecast;_50a‍.w("./utils/forecast.mjs",[["default",function(v){forecast=v}]]);


geocode('Ibiúna', (error, { latitude, longitude, location}) => {
    if (error) {
        return console.log('ERROR :(');
    }

    forecast(latitude, longitude, (nError, nData) => {
        if (nError) {
            return console.log('ERROR :(');
        }

        console.log(location);
        console.log(nData);
    })
})